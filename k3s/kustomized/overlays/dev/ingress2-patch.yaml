apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bots-filebrowser-ingress
spec:
  tls:
    - secretName: bots-edi-tls
      hosts:
        - edifiles-dev.k8.pminc.me
  rules:
    - host: edifiles-dev.k8.pminc.me
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bots-webserver
                port:
                  name: filebrowser
     