# Secrets 

In my deployment secrets are managed by ansible using ansible vault to protect them. you will find the tempates for all required secrets in the files in this directory. You will need to determine you prefered implementation method and security preference.